# Claude Code Settings

## Important Rules

1. **NEVER use `cd` commands** - Always use absolute paths to avoid creating tmpclaude-* files
2. Run commands with full paths: `npm run build --prefix "/full/path/to/project"`
3. Or specify path in the command: `cd "/path" && command` creates temp files - AVOID

## Project: IKINCI KANAL
- Working directory: C:/Users/fatih/Desktop/ikinci kanal
- Always use this full path in all commands
